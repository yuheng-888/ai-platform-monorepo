#!/bin/sh

set -eu

while read -r local_ref local_sha remote_ref remote_sha
do
  case "$remote_ref" in
    refs/heads/main|refs/heads/master)
      echo "Direct pushes to $remote_ref are blocked. Push a feature branch and open a PR instead." >&2
      exit 1
      ;;
  esac
done

exit 0
